{
  "name": "sourcemap-cli-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "decode-sourcemap-cli": "^0.3.5",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.0.0"
}
